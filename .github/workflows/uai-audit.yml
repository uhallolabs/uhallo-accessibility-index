name: UAI Compliance Gateway

on:
  pull_request:
    branches: [ main, master ]

jobs:
  accessibility-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Example: Start your local development server
      # - name: Start App
      #   run: npm run start &
      #   env:
      #     PORT: 3000

      # - name: Wait for server
      #   run: npx wait-on http://localhost:3000

      - name: Run UAI Audit
        # Example URL: Replace with your actual staging/preview URL
        run: npx uhallo-accessibility-index https://example.com
